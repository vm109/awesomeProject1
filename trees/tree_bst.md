# Breadth First Search 
- BST is going through all nodes of a tree in a level
- For a Breadth First Traversal we use queue for traversing 
- Unlike in DFS we use a stack to traverse either inorder, postorder, preorder